server:
  port: 8090

resilience4j:
  circuitbreaker:
    instances:
      circuitApiMethod: # ??? ??? ????? ??
        base-config: default # ??
    configs:
      default:
        ## sliding-window
        sliding-window-type: count_based
        sliding-window-size: 10

        ## fail
        failure-rate-threshold: 20  # open ??? ???? ?? ?? ???(?? ? / ???? ??? ??)
        minimum-number-of-calls: 5  # ?? ??? ?? ?? ?? ?

        ## delay
        slow-call-rate-threshold: 10  # ?? ??? ?? ?? (?? ? / ???? ??? ??)
        slow-call-duration-threshold: 1000 # ???? ??? ??


        ## half-open
        permitted-number-of-calls-in-half-open-state: 10 # half-open?? ?? ??? ???? ?? ?? call ?
        max-wait-duration-in-half-open-state: 0 # half-open ?? ?? ?? (0? ?? ? ??? ?? ?? ? ?? ??)
        wait-duration-in-open-state: 5000ms # open ???? half-open ?? ???? ???? ??
        automatic-transition-from-open-to-half-open-enabled: true # open ???? half-open?? ?? ??(true? ?? ??? ?? ? ?? ??)
        register-health-indicator: true # ?? ?? ?? (actuator)

        ## exception ??
        record-exceptions: # ?? ??? ??? ????? ??? ???? ??? ???? ?? ?
#          - java.io.IOException
#          - java.util.concurrent.TimeoutException
#          - org.springframework.web.client.HttpServerErrorException